import type { MapApproachDescriptor } from '../types.ts'

export const offlineVideoApproach: MapApproachDescriptor = {
  id: 'offline-video',
  label: 'Offline video package',
  description: 'Pre-rendered WebMap videos with reusable offline playback packages.',
}
